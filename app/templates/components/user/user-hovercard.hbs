<img src="{{user.profile.bg_image}}" class="w-full rounded-t-lg" />
<div class="flex justify-center -mt-6">
  <img src="{{user.profile.avatar}}" class="w-16 h-16 rounded-full border-solid border-white border-2 -mt-3">
</div>
<div class="text-center px-3 pb-6 pt-2">
  <h3 class="text-black font-normal font-sans">
    {{user.nickname}}
  </h3>
  <p class="mt-2 font-sans font-light text-sm text-grey-dark">
    {{user.profile.bio}}
  </p>
</div>